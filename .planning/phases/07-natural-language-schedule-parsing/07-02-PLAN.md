---
phase: 07-natural-language-schedule-parsing
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/routes/schedules.ts
  - src/index.ts
  - tests/routes-schedules.test.ts
autonomous: true
requirements:
  - NLP-05
  - NLP-06
  - NLP-07

must_haves:
  truths:
    - "POST /schedules/parse with NL input returns cron expression and human-readable description"
    - "POST /schedules/parse with existing cron expression returns description without LLM call"
    - "POST /schedules/parse with ambiguous input returns warning about low confidence"
    - "POST /schedules/parse with unparseable input returns 422 with guidance message"
    - "POST /schedules/parse with empty input returns 400 validation error"
    - "LLM unavailable returns 503 with fallback suggestion to use raw cron"
  artifacts:
    - path: "src/routes/schedules.ts"
      provides: "Schedule parse route factory"
      exports: ["createScheduleRoutes"]
    - path: "src/index.ts"
      provides: "Wires /schedules route into app"
      contains: "createScheduleRoutes"
    - path: "tests/routes-schedules.test.ts"
      provides: "Route-level tests for /schedules/parse"
      min_lines: 50
  key_links:
    - from: "src/routes/schedules.ts"
      to: "src/services/schedule-parser.ts"
      via: "import parseSchedule for NL translation"
      pattern: "import.*parseSchedule.*schedule-parser"
    - from: "src/routes/schedules.ts"
      to: "src/schemas/schedule-input.ts"
      via: "import parseScheduleBody for request validation"
      pattern: "import.*parseScheduleBody.*schedule-input"
    - from: "src/index.ts"
      to: "src/routes/schedules.ts"
      via: "app.route('/schedules', createScheduleRoutes())"
      pattern: "app\\.route.*schedules.*createScheduleRoutes"
---

<objective>
Create the POST /schedules/parse HTTP endpoint and wire it into the application, completing the natural language schedule parsing feature.

Purpose: Exposes the schedule parser service (from Plan 01) as an API endpoint that users can call to translate natural language schedule descriptions into confirmed cron expressions before using them in agent creation/update.
Output: Schedule routes module, updated index.ts with route mount, and route-level tests.
</objective>

<execution_context>
@C:/Users/dmitr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmitr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-natural-language-schedule-parsing/07-CONTEXT.md
@.planning/phases/07-natural-language-schedule-parsing/07-RESEARCH.md
@.planning/phases/07-natural-language-schedule-parsing/07-01-SUMMARY.md

<interfaces>
<!-- Contracts created in Plan 01 that this plan consumes -->

From src/services/schedule-parser.ts (created in Plan 01):
```typescript
export async function parseSchedule(input: string): Promise<ParseScheduleResponse>;
```

From src/schemas/schedule-input.ts (created in Plan 01):
```typescript
export const parseScheduleBody: z.ZodObject<{ input: z.ZodString }>;
export type ParseScheduleResponse = {
  input: string;
  cronExpression: string;
  humanReadable: string;
  confidence: "high" | "low";
  interpretation: string;
  warning?: string;
};
```

From src/helpers/cron-detect.ts (created in Plan 01):
```typescript
export function isCronExpression(input: string): boolean;
```

<!-- Existing codebase contracts -->

From src/routes/agents.ts (pattern reference for zodErrorHook):
```typescript
function zodErrorHook(result, c) {
  if (!result.success) {
    const details = result.error!.issues.map((issue) => ({
      field: issue.path.join("."),
      message: issue.message,
    }));
    return c.json({ error: "Validation failed", details }, 400);
  }
}
export function createAgentRoutes(db: Database): Hono { ... }
```

From src/index.ts (mount pattern):
```typescript
app.route("/agents", createAgentRoutes(db));
app.route("/health", createHealthRoute(db, getLlmCircuitStatus, startedAt));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create schedule routes and wire into app with tests</name>
  <files>src/routes/schedules.ts, src/index.ts, tests/routes-schedules.test.ts</files>
  <behavior>
    - POST /schedules/parse with { input: "every weekday at 9am" } returns 200 with { input, cronExpression, humanReadable, confidence, interpretation }
    - POST /schedules/parse with { input: "0 9 * * 1-5" } returns 200 with description (no LLM call)
    - POST /schedules/parse with { input: "" } returns 400 with validation error details
    - POST /schedules/parse without body returns 400
    - POST /schedules/parse with ambiguous input returns 200 with confidence "low" and warning field
    - POST /schedules/parse when LLM throws returns 422 with guidance message and suggestions
    - POST /schedules/parse when CircuitBreakerOpenError is thrown returns 503 with fallback guidance
    - GET /schedules/parse returns 404 (only POST supported)
  </behavior>
  <action>
    1. Create `src/routes/schedules.ts` following the factory pattern from `src/routes/agents.ts`:
       - Import `zValidator` from "@hono/zod-validator", `Hono` from "hono".
       - Import `parseScheduleBody` from "../schemas/schedule-input.js".
       - Import `parseSchedule` from "../services/schedule-parser.js".
       - Import `CircuitBreakerOpenError` from "../services/circuit-breaker.js".

       - Create a `zodErrorHook` function matching the pattern in agents.ts (map issues to `{ field, message }` details, return 400).

       - Export `createScheduleRoutes(): Hono` factory function (no db parameter needed -- this route does not access the database).

       - `POST /parse` handler:
         a. Validate body with `zValidator("json", parseScheduleBody, zodErrorHook as never)`.
         b. Extract `input` from validated body.
         c. Call `parseSchedule(input)` in a try/catch.
         d. On success: return `c.json(result)` with 200.
         e. On `CircuitBreakerOpenError`: return `c.json({ error: "Schedule parsing temporarily unavailable", message: "The LLM provider is currently unavailable. Please provide a cron expression directly (e.g., '0 9 * * 1-5')." }, 503)`.
         f. On other errors: return `c.json({ error: "Could not parse schedule", message: "Unable to interpret the schedule description. Try a simpler phrase like 'every weekday at 9am' or 'every 3 hours'.", suggestions: ["every day at 9am", "every weekday at 9am", "every hour", "every Monday at 8am"] }, 422)`.

    2. Update `src/index.ts`:
       - Add import: `import { createScheduleRoutes } from "./routes/schedules.js";`
       - Add route mount after the agents route: `app.route("/schedules", createScheduleRoutes());`
       - No other changes needed. The schedule route has no database or scheduler dependencies.

    3. Create `tests/routes-schedules.test.ts`:
       - Mock `../src/services/schedule-parser.js` to control `parseSchedule` behavior.
       - Create a test Hono app with `createScheduleRoutes()` mounted at `/schedules`.
       - Test: POST /schedules/parse with valid NL input — mock parseSchedule to return success response, verify 200 with correct shape.
       - Test: POST /schedules/parse with cron input — mock parseSchedule to return cron-bypass response, verify 200 with interpretation "Input is already a valid cron expression".
       - Test: POST /schedules/parse with empty input — verify 400 with validation error (Zod rejects empty string).
       - Test: POST /schedules/parse with missing body — verify 400.
       - Test: POST /schedules/parse when parseSchedule returns low confidence — verify response includes warning field.
       - Test: POST /schedules/parse when parseSchedule throws generic Error — verify 422 with guidance message and suggestions array.
       - Test: POST /schedules/parse when parseSchedule throws CircuitBreakerOpenError — verify 503 with fallback message.
       - Use `app.request()` pattern from Hono for making test requests (same pattern as existing route tests).
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/routes-schedules.test.ts -x</automated>
  </verify>
  <done>POST /schedules/parse endpoint is mounted and handles NL input, cron bypass, ambiguity warnings, parse failures (422), and LLM unavailability (503). Full test suite passes including all existing tests with no regressions.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run tests/routes-schedules.test.ts -x` passes
- `pnpm test` (full suite) passes with no regressions
- `POST /schedules/parse` is accessible on the running app
- Route returns correct response shapes for all scenarios
</verification>

<success_criteria>
- POST /schedules/parse with NL input returns 200 with cronExpression, humanReadable, confidence, interpretation
- POST /schedules/parse with cron input returns 200 without invoking LLM
- Invalid/empty input returns 400 with field-level validation errors
- Unparseable NL returns 422 with helpful guidance and suggestions
- LLM outage returns 503 with fallback instruction to use raw cron
- Full test suite passes (no regressions in existing tests)
</success_criteria>

<output>
After completion, create `.planning/phases/07-natural-language-schedule-parsing/07-02-SUMMARY.md`
</output>
