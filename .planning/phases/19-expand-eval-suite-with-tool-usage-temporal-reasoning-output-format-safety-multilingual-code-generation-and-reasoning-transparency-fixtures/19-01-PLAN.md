---
phase: 19-expand-eval-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/fixtures/tool-usage.jsonl
  - evals/fixtures/temporal-reasoning.jsonl
  - evals/fixtures/output-format.jsonl
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
must_haves:
  truths:
    - "tool-usage.jsonl exists with 3 cases that test reasoning about fetched data (not just fetching)"
    - "temporal-reasoning.jsonl exists with 3 cases covering cron explanation, date math, and time zone conversion"
    - "output-format.jsonl exists with 3 cases testing JSON, Markdown table, and CSV structural validity"
    - "All 9 cases load without parse errors via loadAllFixtures()"
    - "No agent name collisions with existing 22 eval agent names"
  artifacts:
    - path: "evals/fixtures/tool-usage.jsonl"
      provides: "3 eval cases testing reasoning about tool-fetched data"
      contains: "tool-01"
    - path: "evals/fixtures/temporal-reasoning.jsonl"
      provides: "3 eval cases testing temporal/scheduling reasoning"
      contains: "temp-01"
    - path: "evals/fixtures/output-format.jsonl"
      provides: "3 eval cases testing structured output format compliance"
      contains: "fmt-01"
  key_links:
    - from: "evals/fixtures/tool-usage.jsonl"
      to: "evals/lib/fixtures.ts"
      via: "JSONL auto-discovery glob (*.jsonl)"
      pattern: "loadAllFixtures"
    - from: "evals/fixtures/temporal-reasoning.jsonl"
      to: "evals/lib/checks.ts"
      via: "CheckDef types with regex, contains, equals"
      pattern: "evaluateCheck"
    - from: "evals/fixtures/output-format.jsonl"
      to: "evals/lib/types.ts"
      via: "EvalCase interface conformance"
      pattern: "EvalCase"
---

<objective>
Create 3 JSONL eval fixture files — tool-usage, temporal-reasoning, and output-format — with 3 cases each (9 total), following the exact patterns established by existing fixtures.

Purpose: Expand eval coverage from 22 to 31 cases across 3 new domains that test reasoning capabilities beyond the existing suite.
Output: 3 new JSONL files in evals/fixtures/ that are auto-discovered by the eval runner.
</objective>

<execution_context>
@C:/Users/dmitr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmitr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-expand-eval-suite-with-tool-usage-temporal-reasoning-output-format-safety-multilingual-code-generation-and-reasoning-transparency-fixtures/19-CONTEXT.md
@.planning/phases/19-expand-eval-suite-with-tool-usage-temporal-reasoning-output-format-safety-multilingual-code-generation-and-reasoning-transparency-fixtures/19-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From evals/lib/types.ts:
```typescript
export interface EvalCase {
  id: string;
  name: string;
  agent: {
    name: string;
    taskDescription: string;
    cronSchedule: string;
    systemPrompt?: string;
    model?: string;
  };
  tags: string[];
  checks: CheckDef[];
  judgeCriteria?: JudgeCriterion[];
  referenceOutput?: string;
  thresholds?: {
    maxDurationMs?: number;
    maxOutputTokens?: number;
    maxCostUsd?: number;
  };
}

export interface CheckDef {
  name: string;
  field: "summary" | "details" | "status" | "data" | "meta";
  type: CheckType;
  params?: Record<string, unknown>;
}

export type CheckType =
  | "equals" | "contains" | "regex" | "minLength" | "maxLength"
  | "notContainsRegex" | "minKeywordCount" | "greaterThan";

export interface JudgeCriterion {
  name: string;
  rubric: string;
  scale: "binary" | "likert";
  minScore: number;
}
```

Existing agent names (MUST NOT reuse):
__eval_jsonapi, __eval_htmlpage, __eval_multiurl, __eval_badurl, __eval_compare,
__eval_convert, __eval_ranking, __eval_impossible, __eval_vague, __eval_minimal,
__eval_pirate, __eval_formal, __eval_list, __eval_analysis, __eval_tutorial,
__eval_compare_db, __eval_chef, __eval_nolist, __eval_historian, __eval_tdd,
__eval_climate, __eval_rest
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tool-usage.jsonl with 3 eval cases</name>
  <files>evals/fixtures/tool-usage.jsonl</files>
  <read_first>
    - evals/fixtures/web-fetch.jsonl (reference pattern for URL-fetching evals — tool-usage MUST differ by testing reasoning/computation, not just extraction)
    - evals/lib/types.ts (EvalCase interface)
    - evals/lib/checks.ts (available check types and their params)
  </read_first>
  <action>
Create `evals/fixtures/tool-usage.jsonl` with exactly 3 JSON lines (one per case). Each line is a single JSON object conforming to EvalCase interface.

**CRITICAL DIFFERENTIATOR:** web-fetch.jsonl tests "can the agent fetch data?". tool-usage.jsonl tests "can the agent REASON about what it fetched?" — computation, anti-hallucination, cross-referencing.

**Case 1 (tool-01, Easy): Compute from fetched JSON data**
- id: `"tool-01"`
- name: `"Calculate from fetched JSON data"`
- agent name: `"__eval_toolcalc"`
- taskDescription: `"Fetch user data from https://jsonplaceholder.typicode.com/users and count how many users have a .org email domain. List their names and email addresses."`
- cronSchedule: `"0 0 * * *"`
- tags: `["tool-usage", "calculation", "data-extraction"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Contains a count: field=meta, type=regex, params={pattern:"\\d+", flags:"i"}
  3. Mentions at least one .org user: field=meta, type=regex, params={pattern:"(Chelsey Dietrich|Mrs\\.? Dennis)", flags:"i"}
  4. Non-trivial output: field=meta, type=minLength, params={min:50}
- judgeCriteria: [{name:"Correct computation", rubric:"Did the agent correctly count users with .org email domains from the fetched data? The jsonplaceholder /users endpoint has users with .org domains. The agent must have actually fetched and analyzed the data, not guessed.", scale:"binary", minScore:1}]
- thresholds: {maxDurationMs: 60000}

**Case 2 (tool-02, Medium): Anti-hallucination — must fetch, cannot know from training**
- id: `"tool-02"`
- name: `"Anti-hallucination data retrieval"`
- agent name: `"__eval_toolfetch"`
- taskDescription: `"Fetch the list of posts from https://jsonplaceholder.typicode.com/posts?userId=3 and tell me exactly how many posts user 3 has. List the titles of the first and last posts in the results."`
- cronSchedule: `"0 0 * * *"`
- tags: `["tool-usage", "anti-hallucination", "must-fetch"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Contains count of 10: field=meta, type=regex, params={pattern:"\\b10\\b", flags:"i"}
  3. Mentions a real post title from user 3: field=meta, type=regex, params={pattern:"(nesciunt quas odio|asperiores ea ipsam|dolorem dolore est)", flags:"i"}
  4. Non-trivial output: field=meta, type=minLength, params={min:40}
- judgeCriteria: [{name:"Fetched not fabricated", rubric:"Did the agent actually fetch the data from the URL and report real titles? User 3 has exactly 10 posts on jsonplaceholder. The first post is 'nesciunt quas odio' (id 21) and the last is 'dolorem dolore est ipsam' (id 30). Fabricated titles or wrong counts indicate hallucination.", scale:"binary", minScore:1}]
- thresholds: {maxDurationMs: 60000}

**Case 3 (tool-03, Hard): Multi-source synthesis**
- id: `"tool-03"`
- name: `"Cross-reference data from multiple sources"`
- agent name: `"__eval_toolsynth"`
- taskDescription: `"Fetch user 5 from https://jsonplaceholder.typicode.com/users/5 and their posts from https://jsonplaceholder.typicode.com/posts?userId=5. What is user 5's name and company? How many posts did they write? What is the average word count across their post titles?"`
- cronSchedule: `"0 0 * * *"`
- tags: `["tool-usage", "synthesis", "multi-source", "calculation"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Mentions user name: field=meta, type=regex, params={pattern:"Chelsey Dietrich", flags:"i"}
  3. Mentions company: field=meta, type=regex, params={pattern:"Keebler", flags:"i"}
  4. Contains post count: field=meta, type=regex, params={pattern:"\\b10\\b", flags:"i"}
  5. Contains word count or average: field=meta, type=regex, params={pattern:"(average|mean|avg).*\\d|\\d.*(average|mean|avg|words)", flags:"i"}
- judgeCriteria: [{name:"Correct synthesis", rubric:"Did the agent correctly combine data from both URLs? User 5 is Chelsey Dietrich from Keebler LLC. They have 10 posts. The average title word count should be computed from the actual titles (approximately 5-7 words per title). The key test is cross-referencing between the two data sources.", scale:"binary", minScore:1}]
- thresholds: {maxDurationMs: 90000}

Each line MUST be valid JSON. No trailing commas, no comments. One JSON object per line, no blank lines.
  </action>
  <verify>
    <automated>cd C:/Users/dmitr/Dev/Schedoodle && node -e "const fs=require('fs'); const lines=fs.readFileSync('evals/fixtures/tool-usage.jsonl','utf8').trim().split('\n'); if(lines.length!==3) throw new Error('Expected 3 lines, got '+lines.length); lines.forEach((l,i)=>{const c=JSON.parse(l); if(!c.id||!c.agent?.name||!c.checks?.length) throw new Error('Line '+(i+1)+' missing fields')}); console.log('OK: 3 valid cases')"</automated>
  </verify>
  <acceptance_criteria>
    - evals/fixtures/tool-usage.jsonl exists with exactly 3 lines
    - Line 1 contains `"id":"tool-01"` and `"__eval_toolcalc"`
    - Line 2 contains `"id":"tool-02"` and `"__eval_toolfetch"`
    - Line 3 contains `"id":"tool-03"` and `"__eval_toolsynth"`
    - All 3 lines parse as valid JSON
    - Every case has field=status type=equals check as first check
    - tags arrays include "tool-usage" in every case
    - No agent name matches any existing __eval_* name
  </acceptance_criteria>
  <done>tool-usage.jsonl contains 3 valid EvalCase objects testing computation, anti-hallucination, and multi-source synthesis</done>
</task>

<task type="auto">
  <name>Task 2: Create temporal-reasoning.jsonl and output-format.jsonl with 3 cases each</name>
  <files>evals/fixtures/temporal-reasoning.jsonl, evals/fixtures/output-format.jsonl</files>
  <read_first>
    - evals/fixtures/data-reasoning.jsonl (reference pattern for numerical/reasoning evals — temporal-reasoning follows similar structure)
    - evals/fixtures/instruction-following.jsonl (reference for structured output constraints — output-format follows similar pattern)
    - evals/lib/types.ts (EvalCase interface)
    - evals/lib/checks.ts (available check types)
  </read_first>
  <action>
Create two JSONL fixture files:

**FILE 1: evals/fixtures/temporal-reasoning.jsonl** (3 lines)

**Case 1 (temp-01, Easy): Explain cron expression**
- id: `"temp-01"`
- name: `"Explain cron expression in plain English"`
- agent name: `"__eval_cronexplain"`
- taskDescription: `"Explain what the cron expression '30 9 * * 1-5' means in plain English. When does it run? Give a clear, concise explanation."`
- cronSchedule: `"0 0 * * *"`
- tags: `["temporal-reasoning", "cron", "explanation"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Mentions 9:30 time: field=meta, type=regex, params={pattern:"9:30|9\\s*30|half past 9|nine.thirty|0?9:30", flags:"i"}
  3. Mentions weekday/Mon-Fri: field=meta, type=regex, params={pattern:"(weekday|monday.*friday|mon.*fri|working day|business day)", flags:"i"}
- judgeCriteria: [{name:"Correct interpretation", rubric:"Does the output correctly explain that '30 9 * * 1-5' means 9:30 AM every weekday (Monday through Friday)?", scale:"binary", minScore:1}]

**Case 2 (temp-02, Medium): Date math calculation**
- id: `"temp-02"`
- name: `"Calculate future date from offset"`
- agent name: `"__eval_datemath"`
- taskDescription: `"If today is March 1, 2026, what date is exactly 45 days later? Show your reasoning step by step."`
- cronSchedule: `"0 0 * * *"`
- tags: `["temporal-reasoning", "date-math", "calculation"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Contains April 15: field=meta, type=regex, params={pattern:"April\\s*15|15\\s*(th)?\\s*(of\\s*)?April|2026-04-15|04/15", flags:"i"}
  3. Shows reasoning: field=meta, type=regex, params={pattern:"(31|30|march|remaining|days|subtract|add)", flags:"i"}
  4. Non-trivial: field=meta, type=minLength, params={min:50}
- judgeCriteria: [{name:"Correct date", rubric:"Is April 15, 2026 correctly calculated? March has 31 days, so 31-1=30 days remain in March, then 15 days into April = April 15. Does the reasoning make sense?", scale:"binary", minScore:1}]

**Case 3 (temp-03, Hard): Time zone conversion with DST awareness**
- id: `"temp-03"`
- name: `"Time zone conversion during DST transition"`
- agent name: `"__eval_timezone"`
- taskDescription: `"A meeting is scheduled for March 8, 2026 at 2:30 PM Eastern Time (US). What time is that in UTC? Note: US Eastern DST (spring forward) begins on March 8, 2026 at 2:00 AM local time. Explain any DST considerations."`
- cronSchedule: `"0 0 * * *"`
- tags: `["temporal-reasoning", "timezone", "dst", "hard"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Contains UTC time (6:30 PM or 18:30): field=meta, type=regex, params={pattern:"(6:30\\s*PM|18:30|1830)\\s*(UTC|Coordinated)", flags:"i"}
  3. Mentions DST or daylight: field=meta, type=regex, params={pattern:"(DST|daylight|spring forward|EDT|Eastern Daylight)", flags:"i"}
  4. Mentions offset change: field=meta, type=regex, params={pattern:"(UTC-4|UTC-5|offset|hour.*(ahead|behind|difference)|4 hours|5 hours)", flags:"i"}
- judgeCriteria: [{name:"DST-aware conversion", rubric:"On March 8 2026, US Eastern DST starts at 2:00 AM (clocks spring forward). By 2:30 PM the region is on EDT (UTC-4), so 2:30 PM EDT = 6:30 PM UTC. Does the agent correctly identify that DST is in effect and compute UTC-4? If the agent says 7:30 PM UTC (UTC-5/EST), that's wrong because DST has already started.", scale:"binary", minScore:1}]
- thresholds: {maxDurationMs: 30000}

**FILE 2: evals/fixtures/output-format.jsonl** (3 lines)

**Case 1 (fmt-01, Easy): Produce valid JSON object**
- id: `"fmt-01"`
- name: `"Produce valid JSON object"`
- agent name: `"__eval_jsonout"`
- taskDescription: `"Create a JSON object representing a book with these exact fields: title (string), author (string), year (number), genres (array of strings). Use any real book you like."`
- cronSchedule: `"0 0 * * *"`
- systemPrompt: `"You MUST output a valid JSON object in your response. No markdown code fences, no explanation text before or after — just the raw JSON object."`
- tags: `["output-format", "json", "structured"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Contains opening brace: field=meta, type=contains, params={text:"{"}
  3. Contains title field: field=meta, type=regex, params={pattern:"\"title\"\\s*:", flags:"i"}
  4. Contains author field: field=meta, type=regex, params={pattern:"\"author\"\\s*:", flags:"i"}
  5. Contains year field: field=meta, type=regex, params={pattern:"\"year\"\\s*:\\s*\\d{4}", flags:"i"}
  6. Contains genres array: field=meta, type=regex, params={pattern:"\"genres\"\\s*:\\s*\\[", flags:"i"}
- judgeCriteria: [{name:"Valid JSON", rubric:"Is the output a valid, parseable JSON object containing all four required fields (title, author, year, genres)? Is the year a number (not string)? Is genres an array?", scale:"binary", minScore:1}]

**Case 2 (fmt-02, Medium): Generate Markdown table**
- id: `"fmt-02"`
- name: `"Generate well-formed Markdown table"`
- agent name: `"__eval_mdtable"`
- taskDescription: `"Create a Markdown table with 3 columns (Language, Year Created, Creator) and exactly 4 rows of data for programming languages. Include the header row and separator row."`
- cronSchedule: `"0 0 * * *"`
- systemPrompt: `"Output ONLY the Markdown table. No explanation before or after. Use standard pipe-delimited Markdown table syntax."`
- tags: `["output-format", "markdown", "table"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Contains pipe-delimited header: field=meta, type=regex, params={pattern:"\\|.*Language.*\\|.*Year.*\\|.*Creator.*\\|", flags:"i"}
  3. Contains separator row: field=meta, type=regex, params={pattern:"\\|\\s*-{2,}\\s*\\|", flags:"i"}
  4. Has at least 4 data rows: field=meta, type=regex, params={pattern:"(\\|.*\\|.*\\|.*\\|.*\\n?){6}", flags:"s"}
  5. Non-trivial: field=meta, type=minLength, params={min:100}
- judgeCriteria: [{name:"Valid Markdown table", rubric:"Is this a well-formed Markdown table with a header row, separator row (dashes), and exactly 4 data rows? Can it be rendered correctly by a Markdown parser?", scale:"binary", minScore:1}]

**Case 3 (fmt-03, Hard): Output valid CSV with escaping**
- id: `"fmt-03"`
- name: `"Output CSV with proper escaping"`
- agent name: `"__eval_csvout"`
- taskDescription: `"Create a CSV file with headers: Name, City, Favorite Quote. Include exactly 3 rows of data. At least one row must have a comma in the Favorite Quote field (requiring proper CSV quoting). At least one row must have a quote character in a field (requiring proper escaping)."`
- cronSchedule: `"0 0 * * *"`
- systemPrompt: `"Output ONLY valid CSV text. No explanation. Follow RFC 4180: fields with commas or quotes must be enclosed in double quotes, and literal quotes within fields must be escaped as double-double-quotes."`
- tags: `["output-format", "csv", "escaping", "hard"]`
- checks:
  1. Returns success: field=status, type=equals, params={expected:"success"}
  2. Contains CSV header: field=meta, type=regex, params={pattern:"Name.*,.*City.*,.*Favorite Quote", flags:"i"}
  3. Contains quoted field (comma in value): field=meta, type=regex, params={pattern:"\"[^\"]*,[^\"]*\"", flags:"s"}
  4. Has at least 3 data lines: field=meta, type=minLength, params={min:100}
- judgeCriteria: [{name:"Valid CSV with escaping", rubric:"Is this valid CSV per RFC 4180? Does it have a header row and 3 data rows? Is at least one field with a comma properly quoted? Is at least one field with a quote character properly escaped with double-double-quotes?", scale:"binary", minScore:1}]

Each file: one JSON object per line, no blank lines, no trailing commas, no comments.
  </action>
  <verify>
    <automated>cd C:/Users/dmitr/Dev/Schedoodle && node -e "const fs=require('fs'); ['temporal-reasoning','output-format'].forEach(f=>{const lines=fs.readFileSync('evals/fixtures/'+f+'.jsonl','utf8').trim().split('\n'); if(lines.length!==3) throw new Error(f+': Expected 3 lines, got '+lines.length); lines.forEach((l,i)=>{const c=JSON.parse(l); if(!c.id||!c.agent?.name||!c.checks?.length) throw new Error(f+' line '+(i+1)+' missing fields')}); console.log(f+': OK 3 valid cases')})"</automated>
  </verify>
  <acceptance_criteria>
    - evals/fixtures/temporal-reasoning.jsonl exists with exactly 3 lines
    - Line 1 contains `"id":"temp-01"` and `"__eval_cronexplain"`
    - Line 2 contains `"id":"temp-02"` and `"__eval_datemath"`
    - Line 3 contains `"id":"temp-03"` and `"__eval_timezone"`
    - evals/fixtures/output-format.jsonl exists with exactly 3 lines
    - Line 1 contains `"id":"fmt-01"` and `"__eval_jsonout"`
    - Line 2 contains `"id":"fmt-02"` and `"__eval_mdtable"`
    - Line 3 contains `"id":"fmt-03"` and `"__eval_csvout"`
    - All 6 lines parse as valid JSON
    - Every case has field=status type=equals as first check
    - tags arrays include "temporal-reasoning" or "output-format" respectively
    - No agent name collisions with existing names or Plan 01 Task 1 names
  </acceptance_criteria>
  <done>temporal-reasoning.jsonl has 3 valid cases (cron explanation, date math, DST conversion) and output-format.jsonl has 3 valid cases (JSON, Markdown table, CSV with escaping)</done>
</task>

</tasks>

<verification>
1. All 3 fixture files exist in evals/fixtures/
2. Each file has exactly 3 lines of valid JSON
3. All 9 cases have unique IDs: tool-01, tool-02, tool-03, temp-01, temp-02, temp-03, fmt-01, fmt-02, fmt-03
4. All 9 agent names are unique and do not collide with existing 22 names
5. Every case starts with a status=success check
6. JSONL loading: `node -e "const {loadAllFixtures}=require('./evals/lib/fixtures.js'); console.log(loadAllFixtures().map(f=>f.file+':'+f.cases.length).join(', '))"`
</verification>

<success_criteria>
- 9 new eval cases across 3 files covering tool-usage reasoning, temporal reasoning, and output format compliance
- All cases follow established JSONL patterns (EvalCase interface, CheckDef types, JudgeCriterion structure)
- tool-usage cases clearly differ from web-fetch (computation and reasoning, not just extraction)
- No parse errors when loaded by eval runner
</success_criteria>

<output>
After completion, create `.planning/phases/19-expand-eval-suite-with-tool-usage-temporal-reasoning-output-format-safety-multilingual-code-generation-and-reasoning-transparency-fixtures/19-01-SUMMARY.md`
</output>
